# DAta RepresenTativeness (DART)

## Getting Started
### Environment
For a list of necessary python packages, see the [requirements file](requirements.txt).
### Flask application
The flask application can be launched for access from localhost by running the command ``` flask --app DART run ``` from the root folder of the repository.
